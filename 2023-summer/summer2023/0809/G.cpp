#include<bits/stdc++.h>
using namespace std;

long long f[200];

int main() {
    // f[1] = f[2] = 1;
    // for(int i = 3; i <= 199; ++i) {
    //     f[i] = f[i-f[i-1]] + f[i-1-f[i-2]];
    // }
    // int n;
    // cin >> n;
    // long long ans = 0;
    // for(int i = 1; i <= n; ++i) {
    //     ans += f[i];
    // }
    // cout << ans << "\n";
    for(int i = 1; i <= 500; ++i) {
        long long ans = 1e18;
        cout << ans << "\n";
    }
}