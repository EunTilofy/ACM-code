#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int n;
    cin >> n;
    string s, t;
    cin >> s >> t;
    int ans = 0, pr = 0;
    for(int i = n - 1; i >= 0; --i) {
        int pos = 
    }
    cout << ans << "\n";
    return 0;
}