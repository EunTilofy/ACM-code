#include <bits/stdc++.h>
using namespace std;

const int N = 2e5 + 1, M = 30 * 30 / 2 + 30 + 1;
int n, m, t;
int f[N][M][M];

int main() {
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);
	cin >> n >> m >> t;
	string s, t;
	vin >> s >> t;
	
	for(int i = 1;	
	return 0;
}