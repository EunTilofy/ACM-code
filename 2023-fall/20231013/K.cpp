#include <bits/stdc++.h>
using namespace std;

int main() {
    int x, y;
    cin >> x >> y;
    if(x > y) cout << "orz teralem is the king!\n";
    else if(x < y) cout << "orz overflowker is the king!\n";
    else cout << "even even seven EIeven.\n";
    return 0;
}